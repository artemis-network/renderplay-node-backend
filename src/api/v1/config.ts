const parentPrefix = "/renderplay/v1"

const userPrefix = parentPrefix + "/users"
const walletPrefix = parentPrefix + "/wallets"
const rendlesPrefix = parentPrefix + "/rendles"
const renderscanPrefix = parentPrefix + "/renderscans"
const renderversePrefix = parentPrefix + "/renderverse"

export {
	userPrefix,
	walletPrefix,
	parentPrefix,
	rendlesPrefix,
	renderscanPrefix,
	renderversePrefix
}